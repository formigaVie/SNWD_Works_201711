16.11.2017:

CSS position tricks - as homework

noch einmal Boogle
Folder Class3/boogle
Pycharm -> Window -> Editor Tab -> Split vertically
Bonus homework 3.4: Boogle login 
Bild von Dort

Div Google
Div Für Schrift (groß / klein)
Div Login (alles zusammen)
	Div Bild
	Div input/btn/frage
Div Create Account
Div footer pics

bei Style.css auch noch style="text/css" ergänzen für eindeutigkeit
alle Classes/ids klein und eindeutig geschrieben

Div zuerst einmal ein Element dass einen Zeilenumbruch mit sich bringt
Wenn keine zusätzliche Angabe über Position dann ist static, dann kommt sie dort wo sie als nächstes möglich wäre
mit position:relative dann zu der Ausgangsposition

font-weight bei Schriften direkt eintragen

google.at
	hex color picker

Wenn zweimal das selbe in einer class/id steht, dann zieht das letzte

im css unter class/id sind properties
im html nach tags sind attributes

bei Bereich wo es verschoben wird (alles innerhalb vom Border) hat auch noch dieselbe Hintergrundfarbe
Margin ist außerhalb vom Border, daher keine graue Schraffierung.

Input = singleton
Button (mit abschließendem tag)

dann stunde 4 Git

git diff zeigt die Änderungen der nicht gestaged files

git diff --staged zeigt nur die "gestageden Dateien

git add .(oder Filename)
git add -p (nur die Einzeländerung wird gefragt und korrigiert)
git commit -m "commit message"
git log 
git push

im Ordner: /home/mani/PycharmProjects/SNWD-patrick/SNWD_201711/SNWD_201711
git status
	wenn branch Master
wenn Mergeconflict dann:
git checkout -- .
git pull (für aktuellen Stand vom Server)
	Zahl -> Zeilen der Änderung zu lokaler Version (+ dazugekommen / - weggekommen)


Excercise 2.1
statt einer Boogle Page ist auch eine andere Seite die wir brauchen werden möglich

Homework 4.3 done:
https://try.github.io/wrap_up

Bonus Homework (GIT)

Bootstrap:
Class5/bootstrap_example

Einbinden von Bootstrap und javascript

Wenn spezielles CSS dann als unterstes in den Header

Refernzieren auf einem lokalen Ordner 
CDN = Content Delivery Network

Link:
http://getbootstrap.com/
Bootstrap 3.3.7

CSS Styles / Grids und coloumns
Components NAVBAR

Bootstrap : Karusell (Slider)
	Akkordeon
	